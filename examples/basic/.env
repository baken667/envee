# This is a regular .env file (dotenv format). It's loaded by envee via `_.file = ".env"`.
# Values here are merged into the env and (unless `redact = true`) are visible in `envee status`.

LOG_FORMAT = "json"
LOG_LEVEL = "info"
FEATURE_FLAG_NEW_UI = "true"
